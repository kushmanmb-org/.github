# Environment variables and sensitive configuration files
.env
.env.*
.env.local
.env.development
.env.test
.env.production
.env.staging
*.local.json
*.local.config
*local.config.json
config.local.*

# API Keys and Credentials
*.apikey
*.api-key
*apikey*
*api-key*
api-keys.*
apikeys.*
*credentials*
*.credentials
secrets.*
*secrets.json
*secrets.yml
*secrets.yaml

# Private keys and certificates
*.key
*.pem
*.p12
*.pfx
*.cer
*.crt
private-key*
*private-key*
privatekey*
*privatekey*

# Blockchain and cryptocurrency wallets
keystore/
*keystore*
wallet.dat
*.wallet
mnemonic.*
*mnemonic*
seed.*
*seed.txt

# Infura and blockchain configuration
infura.config*
*.secrets
alchemy.config*
*-config-private.*

# Etherscan API configuration (contains API keys)
etherscan-api-config.json
*-api-config.json
!*-api-config.example.json

# Beaconcha.in and validator rewards configuration (contains API keys)
validator-rewards-config.json
*validator-rewards-config*
!*validator-rewards-config.example.*
beaconchain-config.*
*beaconchain-config*
!*beaconchain-config.example.*

# Crypto consolidation configuration (may contain sensitive data)
crypto-config.json
*crypto-config.json
!crypto-config.example.json
address-whitelist.json
*address-whitelist*
!*address-whitelist.example.json

# Authentication tokens and session data
*.token
*token.txt
auth-token*
session.*
*.session

# CI/CD secrets
.secrets/
secrets/
*secret.env

# API keys and credentials
*apikey.txt
*apikey.json
*apikey.env
*api-key.txt
*api-key.json
*api-key.env
*api_key.txt
*api_key.json
*api_key.env
.apikey
.apikeys
etherscan.apikey
etherscan-apikey.json
etherscan-apikey.txt
apikeys.json
api-keys.json
api_keys.json
config.local.json
*.local.json
.env.apikey
.env.apikeys

# Node modules and dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build artifacts
dist/
build/
*.log

# Test coverage and artifacts
coverage/
.nyc_output/
*.coverage
.jest-cache/
test-results/
junit.xml

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Python cache
__pycache__/
*.pyc
*.pyo

# Database files (may contain sensitive data)
*.db
*.sqlite
*.sqlite3

# Backup files (may contain sensitive data)
*.backup
*.bak
*.old
*-backup.*

# Local test data and configs
test-data/
local-test.*
*.local.test.*

# Transaction data and hashes (private data)
tx-data/
tx-hashes/
transaction-data.*
transactions.*
*.txdata
*.txhash
private-transactions.*
*private-tx*
# Ignore tx-hashes files but allow example files
*tx-hashes*.json
!*tx-hashes*example*.json
!*example*tx-hashes*.json
*tx-data*.json
!*tx-data*example*.json
!*example*tx-data*.json

# Git verification data (private verification policies and keys)
verification-data/
gpg-verification.*
*verification-config*
*-verification-config.*
!*-verification-config.example.*
!*example*verification*.json
# Ignore commit verification files but allow example files
*commits*.json
!*commits*example*.json
!*example*commits*.json
verification-policy.*
*verification-policy*
!*verification-policy.example.*

# ENS and domain verification data (private ownership proofs)
# Ignore all ens-verification files...
ens-verification.*
*ens-verification*
# ...except example files
!ens-verification-example.*
!*ens-verification*example*
!*example*ens-verification*
ens-ownership.*
*ens-ownership*
domain-verification.*
*domain-verification*
ownership-proof.*
*ownership-proof*

# Base network and ENS configuration (may contain private data)
base-config.*
*base-config*
!*base-config.example.*
ens-config.*
*ens-config*
!*ens-config.example.*

# Smart contract deployment artifacts (may contain sensitive addresses)
deployments/
*deployments/
.openzeppelin/
*.deployed
*deployment-info*
*deploy-info*

# Web3 and blockchain provider configurations
web3-config.*
*web3-config*
!*web3-config.example.*
provider-config.*
*provider-config*
!*provider-config.example.*

# Additional wallet and key patterns
*.keystore
*keystore.json
utxo.db
*.spvchain

# Gas optimization and transaction simulation data
gas-estimates.*
*gas-estimates*
simulation-data.*
*simulation-data*

# Private blockchain data
chaindata/
*chaindata/
private-chain.*
*private-chain*

# Signing keys and encrypted data
*.encrypted
*encrypted.*
signing-key.*
*signing-key*

# Temporary and cache files
*.tmp
*.temp
*~
.cache/
tmp/
temp/
